/**
 * Theme Presets - V2
 * Default palette for the design system
 *
 * @example
 * ```ts
 * import { defaultPalette, createTheme, defaultTheme } from '@aurora-ui/theme'
 *
 * // Use the default palette directly
 * const theme = createTheme(defaultTheme, { colors: defaultPalette })
 * ```
 */

export { defaultPalette } from './default'
