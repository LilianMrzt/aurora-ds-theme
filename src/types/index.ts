// Theme types
export type {
    BaseColors,
    BaseSpacing,
    BaseRadius,
    BaseShadows,
    BaseFontSize,
    BaseFontWeight,
    BaseLineHeight,
    BaseZIndex,
    BaseTransition,
    BaseOpacity,
    Theme,
    ExtendedTheme,
    ExtendTheme,
    DeepPartial,
    ThemeOverride,
} from './theme'

// Color types
export type { ColorScale, ColorName, ColorShade } from './colors'

// Palette types
export type { PaletteName } from './palettes'
