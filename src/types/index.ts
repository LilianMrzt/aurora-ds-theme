// Theme types
export type {
    BaseColors,
    BaseSpacing,
    BaseRadius,
    BaseShadows,
    BaseFontSize,
    BaseFontWeight,
    BaseLineHeight,
    BaseZIndex,
    BaseTransition,
    BaseTheme,
    Theme,
    ExtendTheme,
    DeepPartial,
    ThemeOverride,
} from './theme'

// Color types
export type { ColorScale, ColorName, ColorShade } from './colors'

// Palette types
export type { PaletteName } from './palettes'

